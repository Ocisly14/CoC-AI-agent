# 场景与物品制作规范

版本 1.1 · 配套 [美术手册](README.md)。此文件为制作交接标准，当前修订明确写实油画底材与独立表现性叠加，不表示已有模型或贴图已经完成替换。

## 1. 坐标、尺寸与交接

离线作者资产采用米、Y 向上、物品正面朝 +Z、右侧 +X，静态物品 pivot 放接触底面中心；悬挂物放悬挂点，门的子节点 pivot 放铰链。导出 glTF 2.0/GLB 时烘焙非均匀缩放、核对法线，保留合理命名与材质分组。

现有沙盘使用示意单位，不能把 1 米默认为 1 场景单位。导入器必须依据宿主模型标定 `authorMetersToSceneUnits`，存于作者清单；本轮不为全镇指定虚假的统一比例。已建建筑尺寸沿用当前代码，不用本规范的物品米制建议反向缩放整个城镇。

每个资产交接：作者源文件、GLB、贴图源文件、导出贴图、预览图、清单。清单标明语义 ID、组件关系、作者尺寸、pivot、正面、LOD、材质槽、阴影/剖视职责、贴图空间与来源。源文件按颜色底层、笔触、磨损、文字/标记分层；AI 概念 PNG 本身是扁平参考，不宣称为分层纹理源文件。

命名格式：`gh_<place>_<object>_<variant>_lod<N>`；材质 `mat_<family>_<finish>`；贴图 `<asset>_basecolor|normal|roughness|metallic|ao`。示例 `gh_lighthouse_enamel_mug_a_lod0`。LOD0 为近景、LOD1 街区、LOD2 总览，与 UI 层级名称明确对应。

## 2. 几何预算与 LOD

以下为第一轮作者预算上限，不是已测性能承诺。可见像素收益优先，实际场景仍按设备测量调整。

| 类别 | LOD0 三角形目标 | LOD1 | LOD2 | 要保留的内容 |
|---|---:|---:|---:|---|
| 小物品 | 500–2,000 | 100–500 | 0–80 | 主轮廓、重要孔洞、接触面 |
| 重点小物品 | 1,000–4,000 | 250–1,000 | 0–100 | 功能结构、识别细节 |
| 柜体 / 桩等中物品 | 500–3,000 | 150–700 | 50–150 | 门片/桩头、比例、支撑 |
| 建筑外壳 | 8,000–25,000 | 2,000–8,000 | 500–2,000 | 屋顶、入口、附属翼楼 |
| 单层室内 | 15,000–50,000（含结构，不含独立道具） | 按需隐藏小件 | 不加载 | 地板、墙体、楼梯、主要家具 |

以物品投影包围框最长边为参考：小于 8 px 不显示细小独立物件但保留宿主身份；8–32 px 读剪影；32–96 px 读结构；大于 96 px 展示近景细节。转换有 15% 迟滞。受控重要物件可保留图标/标签，但不能为了可见性把模型夸大成巨物。

六件样板的三级视觉示例见 [物品板](boards/04-objects.png)：近景主图、结构缩略、剪影缩略。小钥匙的总览示例在钉板内合并，而非单独悬浮显示。

森林沿用实例化和现有固定向贴片；近景主干用真实几何与分组树冠。海面使用连续网格和宽阔色带。灯具数量、纹理数和阴影缓存预算以后续实测为准，不以堆灯补足绘画明暗。

## 3. 六件物品的作者卡

表内尺寸全部是便于制作的**美术建议**，模组没有测量值。现有宿主尺度优先；引用事实直接来自对应场景 JSON。

| 样板 / 尺寸 W×D×H（米） | 朝向与支点 | 材质 / 几何 | 磨损与身份 |
|---|---|---|---|
| 搪瓷杯 0.12×0.09×0.10（含把手） | +Z 正面，把手 +X；杯底中心 | 搪瓷钢、裸露钢；杯壁、内腔、把手孔用几何 | 事实：杯沿磕口、内部干净；引用 `item.lighthouse_interior.enamel_mug` |
| 铸铁汤锅 0.44×0.36×0.28（含耳） | +Z 正面，双耳 ±X；锅底中心 | 黑铁、内壁琥珀残留；厚锅沿、耳孔、内腔用几何 | 事实：陈年琥珀汤垢；锅底积炭属美术解释；引用 `item.bluebird_kitchen.stew_pot` |
| 转盘电话 0.25×0.22×0.15 | 拨盘朝 +Z；底脚接触平面中心 | 黑色涂层外壳、拨盘金属、橡胶线；听筒独立子节点 | 事实：黑色柜台转盘电话；握持抛光属美术解释；引用 `item.motel_lobby.desk_phone` |
| 黄铜钥匙 0.025×0.005×0.07 | 齿朝 −Y，正面 +Z；钥匙环悬挂点 | 黄铜；齿形和环孔保留，不绘制可解谜纹样 | 是 `item.motel_lobby.key_board` 的组件，无独立新 ID；4 号空钩必须保留 |
| 木渔具柜 1.20×0.55×1.45 | 柜门朝 +Z；底脚接触面中心 | 裸木、少量铁五金；柜门、脚与主要板结构 | 事实：公用木柜、东西用完归位；盐蚀位置属美术解释；引用 `item.dock.gear_locker` |
| 系缆桩 0.30×0.30×0.85（露出部分） | 主磨槽朝码头泊船方向；地面中心 | 木、绳纤维；桩头与深槽用几何，浅刻痕用贴图 | 引用复数条目 `item.dock.bollards`，实例不自动变成多个新物品 ID |

钥匙样板中的空白挂牌、渔具柜双门、杯体骨白色、锅耳形状为美术方案，不是新增模组事实。系缆桩的船名刻痕在制作板只示意纹理；未获得确切文案前不得生成可读的虚构葡语名字。

| 物品 | 近景 / LOD0 | 街区 / LOD1 | 总览 / LOD2 |
|---|---|---|---|
| 杯 | 杯沿缺口、内腔、把手、材料分区 | 杯体＋把手孔 | 随桌面合并或隐藏 |
| 锅 | 厚沿、双耳、锅腔、汤垢 | 主锅身＋耳轮廓 | 灶具上的暗色团块 |
| 电话 | 听筒、拨盘孔、线圈近景段 | 听筒＋底座＋拨盘色面 | 柜台上的黑色剪影 |
| 钥匙 | 齿、环孔、厚度 | 简化单片，保留悬挂 | 与钉板合并，保留空钩语义 |
| 柜 | 门、合页、脚、板结构 | 双门缝与整体轮廓 | 灰木长方体＋底部接触 |
| 桩 | 桩头、深槽、绳、端面 | 主干＋绳环 | 短粗竖向剪影 |

不要给“查看物品”的 UI 自动增加拿取按钮；是否能拿取由游戏实际可用动作决定。材质选择、几何细节与交互 ID 是不同层次，不从物体画得像什么推断引擎动作。

## 4. 蓝鸟与剖视

外壳：临街两层主楼、较低单层后厨翼楼、阶梯假立面、蓝招牌、橱窗与角板。地板不能与外壳封盖共面；后厨无新增后门。堂座到后厨用弹簧门，楼梯从后厨通往楼上。

楼上：客厅和卧室、面街窗、摇椅、圆桌、五斗柜、铁床和床头灯。概念板中的家具方位只为构图示意，不覆盖已有房间连接、楼梯端点和物件锚点。

作者分组：`shell`、`roof`、`wall_upper`、`wall_low`、`floor`、`stairs`、`contents`、`fixtures`、`shadow_proxy`。组名是离线交接语义，导入器映射到实际工程的剖视机制，不能直接假设现有 runtime 识别这些名字。

上墙/屋顶可让位，地板、低墙、家具、灯具和当前楼梯保持完整。隐藏视觉外壳不能删除建筑物理遮光；阴影壳闭合并保留真实门窗开口。室外近景树干、树冠、根撑分离，保留林下物品。

## 5. 材质与贴图

### 5.1 先完成真实材料底层

基础贴图采用写实油画式的材料描绘，保存真实尺度的木纹、板缝、砖缝、灰泥颗粒、屋面搭接及金属表面。允许克制的可见笔触、局部冷暖变化和细节概括，笔触须服务于材料形态与质地；不预置与材料无关的大幅宽刷、飞溅或厚涂色簇，也不统一覆盖画布纹理。修补、褪漆、锈蚀、积尘的位置与尺度须有材料、使用或环境依据。

区分三种痕迹：建筑施工留下的刷漆痕迹属于真实表面；用油画笔触描绘木纹或斑驳旧漆属于底材的绘画表达；跨越几块板的大色簇、骨白破边属于后续表现层。选择照片、扫描、程序或绘制来源均可，最终统一为材质可信、尺度正确、带克制手绘质感的底材。

八类基础见 [materials.json](materials.json)。粗糙度为 0–1 的作者建议，法线强度为相对单位。金属度按真实材质使用：裸金属 1，漆/锈/搪瓷 0，通过遮罩区分，不用整物件中间值混成塑料。

基色与可见发光贴图使用 sRGB；normal/roughness/metallic/AO 为线性数据。法线使用 glTF / OpenGL +Y 约定。AO 只保留克制的局部接触与缝隙，不固化大面积方向阴影。基础色不画入夕阳高光、窗光或长投影。

### 5.2 用实际覆盖宽高校准纹理

2K 图集、1K 独立纹理与非重点 512 图仅为资源预算起点，不代表其应覆盖的面积。每种基础材质记录 `coverageMeters: [W, H]`、源图像素宽高、材料单元尺寸（如挂板露出高度）、纹理方向及平铺或专用 UV 用途。这里是交接字段约定，不表示现有导入器已经读取这些字段。

同材质表面的 UV 按作者米制统一。假设一张底图覆盖 2×2 米，含 10 行等高挂板，则每行露出高度为 0.2 米；一面 6×3 米的墙应对应 3×1.5 个纹理周期，约 15 行板，不能把整图拉伸成 6×3 米。该数值仅为校准示例，不是蓝鸟实测尺寸或通用建筑标准。窗套随自身构件方向映射，屋面按坡面实际长度计算，不能只按俯视投影面积铺图。

分别计算横、纵纹素密度：`像素宽 / 覆盖宽米` 与 `像素高 / 覆盖高米`。材料单元大小先成立，再依据近景需求和显存预算调整像素密度；不能通过放大木纹来补分辨率。方图覆盖长方形表面时，源图内容需按该目标比例制作并验证。非均匀对象缩放须烘焙或补偿，楼层间、转角及邻接构件不得突变板宽或拉长颗粒。

基础材料可以无缝平铺，接缝、污损与局部修补用独立区域处理以减少重复；油画图章按构图放置，不与底材共用 Repeat。图集单元留至少 16 px 扩边（以 2K 源尺寸计）；低 mip 层仍需检查串色，必要时分离贴图。

### 5.3 在材料成立后添加油画层

纹理源文件保留带自然旧化的基础材料、绘画色块／笔触与文字层，额外定点磨损可单独编辑。生成底图时就包含适量真实损伤；无需将已有损伤强行拆层，也不能把去除小笔刷色块做成清洗全部旧化。关闭表现性叠加层时应露出完整写实油画底材，保留描绘材料的克制笔触；不得残留旧版与材料无关的大色簇，也不能靠模糊旧图修复材料尺度。现有大色块的形状、相近色与跨构件投射思路可继续使用，需在新底材上重新判断覆盖与对比。

绘画层包含少量宽色面、擦色与破边，以稳定表面坐标和独立世界尺寸定位；材料身份及板缝应通过安静区与破碎边缘持续可读。几何与基础 normal 保留真实凹凸，不能从绘画颜色自动生成高度。动态光照继续负责日照、高光与投影，绘画层也不固定烘入另一套太阳阴影。详细投射方法见[局部绘画与材质交付](skills/painterly-environment/references/paint-layering.md)。

源文件可编辑分层；交付可以为目标渲染链路烘焙合成图，但必须另存基础版本和绘画遮罩。输出 PNG 为交换格式，后续 Web 发布可以评估 KTX2。一个 2048² RGBA8 完整 mip 链约 21.33 MiB，一个 1024² 约 5.33 MiB，压缩文件大小不代表显存；避免无目的堆叠独立 2K 图。

## 6. 生产顺序与质量门

1. 从现有模块、布局和模型确认结构，记录事实/美术建议。
2. 灰盒验证比例、落地、轮廓、通道、剖视与拾取代理。
3. 绘制同一构图的灰度与色稿，按视觉板调整大关系。
4. 完成影响轮廓、投影与接触的建筑构造细节。
5. 铺真实材料底图，按实际覆盖宽高校准 UV，完成有依据的磨损与表面响应；先验收仅基础材质版本。
6. 独立叠加大色块、方向笔触与破边，保留写实绘画底纹；同机位、同灯光、同曝光对照基础版与合成版，并提供绘画层遮罩。
7. 制作 LOD 和材质共享，核查远景闪烁与贴图尺度；基础层与叠加层均检查近、中、远视距，绘画层开关不得改变板宽或几何。
8. 六时刻、室内日夜、海雾和近景检查；核查说明文字与图像不泄漏模组事实。
9. 工程集成后在真实设备检查资源释放、帧时、首次加载与快速切层。

只对机器可验证项做自动检查：引用 ID、尺寸字段、色彩空间、token 对比度、SVG 几何和文件完整性。艺术一致性、灰度层次与可读性必须看图，不能靠测试数量宣称通过。
